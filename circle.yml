machine:
    services:
        - docker

dependencies:
    post:
        - docker build -t joke/psychosis .

test:
    post:
        - docker run -d joke/psychosis sleep 10


deployment:
    production:
        branch: master
        commands:
            - .circleci/run_circleCI.sh

machine:
    services:
        - docker

dependencies:
    post:
        - docker build -t .

test:
    post:
        - docker run -d joke/psychosis; sleep 10


deployment:
    production:
        branch: master
        commands:
            - run_circleCI.sh
